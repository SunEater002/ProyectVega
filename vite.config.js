import { defineConfig } from 'vite';

export default defineConfig({
  root: '.', // Asegúrate de que el directorio raíz sea correcto
  build: {
    outDir: 'dist', // Directorio de salida para la compilación
  },
});